<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="model"
            type="com.github.ttanaka330.learning.webapi.openweathermap.model.Weather" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <ImageView
            android:id="@+id/image_weather"
            android:layout_width="72dp"
            android:layout_height="72dp"
            app:imageUrl="@{model.icon}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="ContentDescription"
            tools:src="@tools:sample/avatars" />

        <TextView
            android:id="@+id/text_datetime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginLeft="8dp"
            android:minLines="1"
            android:text="@{model.datetime}"
            app:layout_constraintBottom_toTopOf="@+id/text_weather"
            app:layout_constraintStart_toEndOf="@+id/image_weather"
            app:layout_constraintTop_toTopOf="@+id/image_weather"
            tools:text="@tools:sample/date/ddmmyy" />

        <TextView
            android:id="@+id/text_weather"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minLines="1"
            android:text="@{model.weather}"
            app:layout_constraintBottom_toTopOf="@+id/text_celsius"
            app:layout_constraintStart_toStartOf="@+id/text_datetime"
            app:layout_constraintTop_toBottomOf="@+id/text_datetime"
            tools:text="@tools:sample/full_names" />

        <TextView
            android:id="@+id/text_celsius"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minLines="1"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@+id/text_weather"
            app:layout_constraintTop_toBottomOf="@+id/text_weather"
            app:textKelvinToCelsius="@{model.kelvin}"
            tools:text="@tools:sample/us_phones" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>